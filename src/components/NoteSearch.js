import React from "react";

function NoteSearch() {
    return (
        <div className="note-search">
            <input type="text" placeholder="Cari catatan ..." autoFocus autoComplete="off" />
        </div>
    );
};

export default NoteSearch;